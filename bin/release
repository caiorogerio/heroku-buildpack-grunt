#!/usr/bin/env bash
# bin/release <build-dir>

cat << EOF
default_process_types:
  web:
    npm install --dev
    npm install -g grunt-cli
    npm install -g bower
    bower install
    grunt build
    sh boot.sh
EOF
